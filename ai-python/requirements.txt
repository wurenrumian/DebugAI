# 核心 web / API 框架（升级后性能更好、文档更现代）
fastapi>=0.135.0
uvicorn[standard]>=0.41.0

# OpenAI 客户端（强烈建议升，但代码要适配 v2 API）
openai>=2.24.0

# 数据验证（保持最新，FastAPI 官方推荐）
pydantic>=2.12.0

# 环境变量
python-dotenv>=1.0.1

# 测试框架（建议一起升级）
pytest>=8.0
pytest-asyncio>=0.24.0

# HTTP 客户端（新版更安全、支持更好）
httpx>=0.28.0

# 日志（基本无风险）
structlog>=24.1.0