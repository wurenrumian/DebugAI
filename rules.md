
# 📜 项目协作与开发规范 (rules.md)

> **核心原则：** 文档先行，AI 辅助，测试守底，Git 留痕。

---

## 🛠 一、 Git 协作规范与常用命令

### 1. 核心习惯：原子化提交 (Atomic Commits)

**严禁积攒一整天的代码才进行一次提交。**

* **阶段性提交：** 每完成一个功能点、修复一个微小 Bug 或重构完一个函数，就进行一次 `add` 和 `commit`。
* **好处：** 方便回滚（Undo）、减少合并冲突、让 AI 辅助工具更好地理解你的代码增量。

### 2. 常用命令清单

如果你不确定下一步做什么，按这个流程走：

| 场景 | 命令 | 备注 |
| --- | --- | --- |
| **开始工作前** | `git pull origin main` | 先同步远程代码，防止冲突 |
| **创建新功能分支** | `git checkout -b feat/my-feature` | 永远不在 main 上直接写 |
| **查看修改状态** | `git status` | 养成随时查看状态的习惯 |
| **阶段性暂存** | `git add .` | 完成一个小点就 add 一次 |
| **提交到本地** | `git commit -m "feat: 描述"` | 记录你的阶段性成果 |
| **推送至云端** | `git push origin feat/my-feature` | 准备发起 PR |
| **救急：撤销 add** | `git reset HEAD <file>` | 发现 add 错了文件时使用 |

### 3. 分支与合并

* **分支命名：** `feat/功能`、`fix/问题`、`docs/文档`。
* **合并流程：** 在 GitHub/GitLab 发起 **Pull Request (PR)**，由队友 Review 后合并。

---

## 🤖 二、 Vibecoding & AI 使用守则

1. **尸检报告 (Post-Mortem)：**
* 若某个 Bug 导致阻塞超过 15 分钟，必须在 `docs/AI_LOG.md` 记录。
* 记录：**错误现象**、**AI 的错误尝试**、**最终有效的 Prompt**。


2. **代码 Review：**
* 严禁直接粘贴 AI 生成的代码而不阅读逻辑。
* 重点检查：AI 是否引入了不存在的库、是否有逻辑死循环。



---

## 🧪 三、 模块化与单元测试

1. **目录要求：** 每个 `modules/xxx/` 下必须有 `tests/` 目录。
2. **测试先行：** 每次要求 AI 写功能代码前，先问它：*“请先告诉我这段逻辑的测试用例该怎么写？”*。
3. **合并标准：** PR 必须包含测试用例，且本地测试通过。

---

## 📖 四、 模块文档规范 (README)

每个模块目录下必须包含 `README.md`，定义接口契约：

1. **功能描述：** 模块的用途。
2. **接口定义 (API)：** 函数名、输入参数、返回值。
* *协作原则：两人先对齐 README 里的接口名，再各自找 AI 撸代码。*


3. **外部依赖：** 记录该模块用到的特殊第三方库。

---

* **多 commit，少大改：** 小步快跑是最稳妥的协作方式。
* **不要怕冲突：** 遇到 Git 冲突 (Merge Conflict) 时，那是学习 Git 最快的时候，找学长带你合一次就懂了。
* **调教 AI：** 如果 AI 给的代码跑不通，不要盲目重试，尝试给它更多的 Context（上下文）或者具体的报错信息。

